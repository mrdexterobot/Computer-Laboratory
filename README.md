# Computer Laboratory System

## Default accounts

Use these seeded demo accounts for local testing:

- Administrator: `admin` / `Admin@123`
- Faculty: `msantos` / `Faculty@123`
- Faculty: `jreyes` / `Faculty@123`
- Faculty: `acruz` / `Faculty@123`

## Notes

- The main PHP app lives under `comlab_sms/comlab_v7`.
- These credentials match the demo accounts shown on `comlab_sms/comlab_v7/login.php` and the seeded logins documented in `comlab_sms/comlab_v7/docs/department-integration-endpoints.md`.
